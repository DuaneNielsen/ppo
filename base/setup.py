#!/usr/bin/env python
from setuptools import setup

setup(name='duanesbase',
      version='0.0.1',
      install_requires=['torch',
                        'numpy',
                        'torchvision']
      )