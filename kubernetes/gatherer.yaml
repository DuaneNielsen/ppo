apiVersion: apps/v1
kind: Deployment
metadata:
  name: gatherer
spec:
  replicas: 1
  selector:
    matchLabels:
      component: gatherer
      type: ppo
  template:
    metadata:
      labels:
        component: gatherer
        type: ppo
    spec:
      containers:
        - name: gatherer
          image: 127.0.0.1:32000/ppo
          command: [ "/bin/bash", "-c", "--" ]
          args: ["python3 /ppo/controller.py -g --redis-host=fair-squirrel-redis-master.default.svc.cluster.local --redis-password=${REDIS_PASSWORD}"]
          env:
            - name: MY_POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fair-squirrel-redis
                  key: redis-password
          resources:
            limits:
              cpu: 2
